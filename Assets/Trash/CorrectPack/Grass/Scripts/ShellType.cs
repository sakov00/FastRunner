namespace ShellTexturedGrass
{
    public enum ShellType
    {
        VerticalStack, // pile of textures on top of each other
        LayerStack // pile of textures on multiple meshes, each mesh is bigger than the last
    }
}